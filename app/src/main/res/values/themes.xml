<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CalCulator" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.CalCulator" parent="Base.Theme.CalCulator" />

    <style name="TextStyle" parent="Base.Theme.CalCulator">
        <item name="android:textColor">@color/text_color</item>
        <item name="android:fontFamily">@font/doppio_one_regular</item>
    </style>

    <style name="ButtonStyle" parent="Widget.AppCompat.Button">
        <item name="android:fontFamily">@font/doppio_one_regular</item>
        <item name="android:background">@drawable/button_bg</item>
        <item name="android:textSize">22sp</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="ButtonStyle.Operation">
        <item name="android:textColor">@color/background_color</item>
        <item name="android:backgroundTint">@color/brand_color</item>
    </style>

    <style name="ButtonStyle.Number">
        <item name="android:textColor">@color/text_color</item>
        <item name="android:backgroundTint">@color/background_color</item>
    </style>
</resources>